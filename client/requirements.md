## Packages
react-rnd | Drag and drop, resizable components for the presentation canvas
date-fns | Date formatting for dashboard items

## Notes
The canvas uses a fixed 16:9 aspect ratio (1024x576) internally, scaled down if needed.
Element styles (x, y, width, height) are stored in the `style` jsonb column.
